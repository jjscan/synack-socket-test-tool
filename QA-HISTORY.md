# QA / 부하 테스트 이력

이 문서는 **다음에 이 저장소를 만지는 사람(또는 AI 에이전트)** 을 위한 검증 기록입니다.
"무엇이 이미 검증됐는지", "어떤 결함이 있었고 왜 그렇게 고쳤는지", "무엇이 의도된 동작이라 고치면 안 되는지"를 담고 있습니다.

이 저장소에는 CI가 없습니다. 검증은 **실제 앱을 띄우고 실제 소켓을 열어** 수행하며,
그 하네스는 **저장소 안 [`qa/`](qa/README.md) 에 들어 있습니다.**

```bash
dotnet build                        # 본체를 먼저 빌드
dotnet run --project qa/FunctionalQa   # 기능 158개
dotnet run --project qa/StressTest     # 부하 42개
```

---

## 1. 현재 검증 상태 (2026-08-26 기준)

| 항목 | 값 |
| --- | --- |
| 검증한 버전 | **v2.0.1** (`FileVersion 2.0.1.0`) |
| 빌드 | 오류 0개 / 경고 230개 (전부 `<Nullable>enable</Nullable>`로 인한 CS86xx 계열. 이 수치가 기준선이며, 새 경고가 생기면 회귀입니다) |
| 기능 QA | **158개 검사 전부 통과** (16개 구간) |
| 부하·자원 테스트 | **42개 검사 전부 통과** (9개 구간) |
| 실행 환경 | Windows 10 Pro 19045 / 12 논리 코어 / 32 GB RAM / .NET SDK 10.0.101 (타깃 net8.0-windows) |

> 두 스위트 모두 **실제 앱을 띄우고 실제 소켓을 127.0.0.1에 열어** 수행했습니다.
> 모킹이나 서비스 단위 테스트가 아닙니다.

---

## 2. 기능 QA 결과 (158개 검사)

| 구간 | 검사 수 | 검증 내용 |
| --- | ---: | --- |
| A. 버전 표기 | 8 | 어셈블리 버전 = 상태 표시줄 = 릴리스 기록 최신 항목 일치, 버전 기록 창 |
| B. 빈 상태 화면 | 6 | 진입점 3개, 연결 생성 시 사라짐 |
| C. 연결 추가 대화상자 | 22 | 서버/클라이언트 모드 전환, 응답 패턴 카드, 규칙 표, 자동 전달, 입력→결과 매핑, 검증(잘못된 IP·자기 자신 전달), 수정 모드 |
| D. 연결 목록 관리 | 11 | Seq 재정렬, 제거, 다중 선택 일괄 시작·중지, 커맨드 활성화, 중복 서버 차단 |
| E. 서버 응답 패턴 | 8 | Echo / SendOnce / ReplyAfterReceive(1회·지속) / ListenOnly |
| F. 규칙 기반 응답 | 4 | 규칙 2종, 규칙이 Echo보다 우선 |
| G. 수신 조각 합치기 | 3 | ReceiveTimeout 내 조각 병합, 침묵 후 분리 |
| H. 전송 기능 | 14 | 1회 전송, 제어문자 태그→실제 바이트, 주기 전송, 서버 브로드캐스트 |
| I. 인코딩 | 7 | ASCII / UTF-8 / EUC-KR 왕복 및 바이트 수 |
| J. 로그 기능 | 15 | 시각·길이·본문, 보기 모드 3종, 검색 일치 수, Clear, 순환 버퍼 |
| K. 파일 로깅 | 4 | 실시간 로그 파일 생성·내용, 내보내기 형식 |
| L. 자동 전달 | 6 | 원본 바이트 중계, 대상 다운 시 큐 보관 후 순서 유지 재전송 |
| M. 실패 처리와 배너 | 12 | 포트 점유 배너·원인 코드·점유 프로세스 조회·재시도, 접속 실패, 상대 종료, 배너 상한 |
| N. 세션 저장/불러오기 | 12 | JSON 왕복, `[JsonIgnore]` 준수, AutoStart, 최근 세션 |
| O. 테마 | 15 | 라이트/다크 팔레트 교체, 메뉴 팝업 명암비, 저장·복원 |
| P. 자원 정리 / 경합 | 9 | 포트 반납, 중지 경합 6회 반복, 연속 시작 방지 |

**메뉴 팝업 가독성 실측** (WCAG 명암비, 본문 권장 4.5:1)

| | 다크 | 라이트 |
| --- | ---: | ---: |
| 메뉴 / 하위 메뉴 | 12.6:1 | 17.2:1 |
| 컨텍스트 메뉴 | 12.6:1 | — |
| 툴팁 | 12.6:1 | — |

---

## 3. 부하 / 자원 테스트 결과 (42개 검사)

### 3.1 처리량·안정성

| 시나리오 | 결과 |
| --- | --- |
| 고속 대량 수신 20,000건 (520 KB) | 전량 수신, **손실 0** / UI 최대 지연 1.7초 이내 |
| 대용량 페이로드 1 MB × 3건 | 전량 수신 / UI 최대 **52 ms** / 메모리 +13 MB |
| 클라이언트 60개 동시 접속 × 200건 | 216,000바이트 전량 수신 / UI 17 ms / 종료 후 핸들 회수 |
| 10 ms 주기 전송 × 10연결, 5초 | 연결당 321회 지속 (실측 간격 15.6 ms — 4.2절 참고) |
| 로그 30,000건 폭주 | 순환 버퍼 1,000건 유지 / GC힙 33 MB |
| 자동 전달 대상 다운 상태 폭주 | 큐 상한 준수 / 메모리 증가 없음 / 서버 계속 동작 |

### 3.2 연결 수 확장 (자원 한계 탐색)

서버+클라이언트 쌍을 늘려가며 측정. 모두 정상 연결(Error 0건).

| 연결쌍 | 프로세스 | GC힙 | CPU | 핸들 | 스레드 | 시스템 여유 | UI 지연 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 100 | 218 MB | 40 MB | 3.3% | 1,564 | 34 | 19,496 MB | 70 ms |
| 400 | 266 MB | 41 MB | 2.2% | 2,763 | 34 | 19,441 MB | 70 ms |
| 800 | 334 MB | 58 MB | 3.6% | 4,363 | 34 | 19,379 MB | 77 ms |
| 1,600 | 440 MB | 72 MB | 4.3% | 7,528 | 26 | 19,269 MB | 77 ms |
| 2,400 | 554 MB | 88 MB | 4.5% | 10,724 | 26 | 19,172 MB | 103 ms |
| 3,200 | 665 MB | 116 MB | 4.8% | 13,916 | 24 | 19,057 MB | 163 ms |
| 4,000 | 789 MB | 132 MB | 5.6% | 17,113 | 23 | 18,975 MB | 186 ms |
| 5,000 | 934 MB | 158 MB | 5.9% | 21,109 | 20 | 18,818 MB | 186 ms |
| **6,000** | **1,117 MB** | 175 MB | 7.1% | **25,109** | 20 | 18,641 MB | 221 ms |

- 6,000쌍 = 연결 객체 12,000개 = 소켓 약 18,000개
- **연결쌍당 약 0.17 MB / 핸들 4개** — 증가가 선형이고 저렴합니다
- 12,000개 연결을 **열어 둔 유휴 상태 CPU: 0.0%** (코어 1개분 = 8.3%)
- 시스템 여유 메모리는 19.6 GB → 18.6 GB, **1 GB만 감소**
- 정리 후 프로세스 261 MB / 핸들 1,147로 기준선(113 MB / 1,055) 근처까지 회수, 직후 새 서버 생성·통신 정상

### 3.3 결론 — OS 자원 고갈 가능성

**이 앱으로는 이 PC의 자원을 고갈시킬 수 없습니다.**

- 32 GB를 위협하려면 10만 쌍 이상 필요 (선형 외삽)
- 그보다 먼저 **Windows 동적 포트 범위(약 16,384개)** 가 바닥납니다 → 예상 한계 약 8,000쌍
- 즉 제약은 RAM/CPU가 아니라 **TCP 포트**이며, 그 시점에는 소켓 예외가 발생해
  앱이 죽는 대신 **해당 연결만 `Error` 상태 + 인라인 배너**로 표시됩니다
  (포트 점유 실패 경로로 이미 검증됨 — 기능 QA M구간)

> **의도적으로 하지 않은 것**: 포트 범위를 완전히 소진하는 테스트.
> 소진 중에는 이 PC의 **다른 프로그램 outbound 연결도 실패**하고,
> OS를 실제로 멈추면 하드 리셋으로 무관한 작업이 손실될 수 있습니다.
> 하네스에는 자동 중단 한계선(시스템 여유 4 GB / 프로세스 4 GB / 핸들 5만 / 스레드 3천 / UI 5초)을
> 상수로 노출해 두었으니, 더 밀어보려면 그 값과 `MaxConnectionsToTry`만 올리면 됩니다.

---

## 4. 반드시 알아야 할 것 — 고치면 안 되는 동작

아래는 **버그가 아니라 의도된 동작이거나 환경 제약**입니다. 결함으로 오해하고 "고치지" 마세요.

### 4.1 표시용 문자열 4 KB 상한 (의도적)

`LogEntry.DisplayByteLimit = 4096`. 원본 바이트(`Data`)는 항상 완전하게 보관하지만
화면·로그 파일에 쓰이는 문자열은 앞 4 KB까지만 만들고 `… (총 N 바이트 중 4,096 바이트만 표시)`를 붙입니다.
상한을 없애면 1 MB 메시지 한 건이 UI를 10초 이상 멈춥니다(5.9절 참고).

### 4.2 주기 전송의 실질 하한은 약 15.6 ms

Windows 기본 타이머 해상도 때문에 10 ms를 지정해도 실측은 약 15.6 ms입니다.
`System.Timers.Timer`의 한계이며 앱 결함이 아닙니다.
(`timeBeginPeriod`로 시스템 전역 타이머 해상도를 올리는 것은 권장되지 않아 하지 않았습니다.)

### 4.3 다크 제목 표시줄은 Windows 10에서 동작하지 않음

`DWMWA_USE_IMMERSIVE_DARK_MODE`(속성 20)는 빌드 19045에서 **S_OK를 반환하지만 실제로는 적용되지 않습니다** —
시스템 앱 모드가 어두울 때만 반영되고, 무조건 적용은 Windows 11부터입니다.
확인 후 해당 코드는 **제거**했습니다. 다시 넣지 마세요. 필요하면 커스텀 타이틀바로 가야 합니다.

### 4.4 `0.0.0.0`과 `127.0.0.1`은 같은 포트에 동시 바인딩됩니다

Windows 동작입니다. **포트 충돌 테스트를 이 조합으로 짜면 재현되지 않습니다.**
충돌을 재현하려면 외부 `TcpListener`로 **같은 주소**를 점유해야 합니다.

### 4.5 그 밖의 의도된 상한

- 연결별 UI 로그 **1,000건** 순환 버퍼 (오래된 것부터 삭제, `MessageCount`는 계속 누적)
- 자동 전달 큐 **1,000건** 상한 (초과 시 오래된 것부터 폐기, 100건 단위로 로그 경고)
- 인라인 배너 최대 **3건**

### 4.6 자동 전달의 원리적 한계

쓰기 직전에 `IsPeerClosed()`로 상대 종료를 감지하지만, **검사 직후에 상대가 죽는 찰나**에는
TCP가 그 쓰기를 성공으로 보고하므로 1건이 유실될 수 있습니다.
애플리케이션 레벨 ACK 없이는 제거할 수 없는 한계입니다.

---

## 5. 검증 과정에서 발견해 고친 결함

회귀 방지를 위해 남깁니다. **같은 실수를 되돌리지 않도록** 각 항목의 "왜"를 함께 적었습니다.

| # | 결함 | 원인 | 수정 | 버전 |
| ---: | --- | --- | --- | --- |
| 1 | 앱이 '응답 없음'으로 멈춤 | `_ = serverManager.Start(...)`를 UI 스레드에서 호출 → 모든 `await` 재개 지점이 UI 스레드로 복귀 | `Task.Run`으로 시작, 모든 await에 `ConfigureAwait(false)`, 상태 갱신은 `BeginInvoke` | 2.0.0 |
| 2 | Check 버튼이 창 전체를 멈춤 | `async` 선언이지만 `await`가 없어 UI 스레드에서 동기 실행 | `Task.Run`으로 분리 | 2.0.0 |
| 3 | Edit 충돌 시 소켓 누수 | 중지한 적 없는 연결에 `StartConnection` 재호출 → 이전 리스너가 포트를 쥔 채 버려짐 | 잘못된 재시작 제거 + `StartConnection`에 살아있는 Manager 정리 가드 | 2.0.0 |
| 4 | 자동 전달 중 1건 유실 | TCP가 죽은 상대에 대한 첫 쓰기를 성공으로 보고 → 큐에서 제거됨 | 쓰기 전 `IsPeerClosed()` 검사, 성공 후에만 큐에서 제거 | 2.0.0 |
| 5 | 다크 모드에서 메뉴 글자가 안 보임 | `MenuItem`에 스타일이 없어 팝업이 시스템 기본 흰 배경 + 상속된 밝은 글자 | `MenuItem`(4개 Role) / `ContextMenu` / `ToolTip` / 구분선 암시적 스타일 추가 | 2.0.1 |
| 6 | 중지했는데 상태가 `Error`, 가짜 배너 | 리스너를 닫으면 대기 중이던 `Accept`가 예외로 깨어나는데 `OperationAborted`만 정상 처리 | `_isStopping` 플래그를 리스너 닫기 전에 세우고, 중지 중 예외는 종류 불문 정상 종료 처리 | 2.0.1 |
| 7 | 연속 시작 시 가짜 포트 충돌 | 소켓 바인딩이 `Task.Run` 안이라 `IsRunning`이 아직 false → 두 번째 시작이 가드 통과 | 시작 즉시 동기적으로 `Status = "Starting"` | 2.0.1 |
| 8 | Hex 문자열 끝에 공백 | 구분 공백을 매 바이트 뒤에 붙임 | 두 번째 바이트부터 앞에 붙이도록 변경 | 2.0.1 |
| 9 | 1 MB 메시지에 UI 10초 정지, 메모리 810 MB | ①`Take(n)` 바이트 단위 열거 ②전체 디코딩(2 MB)+기호 렌더링(2 MB) ③Hex 3 MB (숨겨진 요소도 바인딩 평가됨) ④거대 문자열 TextBlock 배치 | 표시용 문자열 4 KB 상한 + `ArraySegment`/`AsSpan` 배열 복사 | 2.0.1 |

**#9 개선 폭**: UI 최대 정지 10,001 ms → **52 ms**, 프로세스 메모리 810 MB → **196 MB**

---

## 6. 검증 도구

하네스는 저장소 안에 있습니다. 실행 방법과 안전 한계선은 **[qa/README.md](qa/README.md)** 를 보세요.

| 프로젝트 | 내용 | 검사 수 |
| --- | --- | ---: |
| `qa/FunctionalQa` | 앱이 제공하는 모든 기능 (16개 구간) | 158 |
| `qa/StressTest` | 부하와 자원 한계 (9개 구간) | 42 |

아래는 새 하네스를 추가하거나 구조를 이해할 때 필요한 배경입니다.

### 6.1 공통 골격

`SocketTestTool.csproj`는 `.cs`를 기본 글롭으로 컴파일하므로, 하네스가 저장소 안에 있으면
`Main`이 중복되어 **앱 빌드가 깨집니다.** 그래서 본체 csproj에 다음이 들어 있습니다.

```xml
<Compile Remove="qa\**" />
<Page Remove="qa\**" />
<None Remove="qa\**" />
<EmbeddedResource Remove="qa\**" />
```

새 하네스를 만들 때도 `qa/` 아래에 두면 자동으로 제외됩니다.

본체는 `<SelfContained>true</SelfContained>`라서 `ProjectReference`로 참조할 수 없습니다
(`NETSDK1150`). 빌드된 DLL을 상대 경로로 직접 참조합니다.

```xml
<Reference Include="SocketTestTool">
  <HintPath>..\..\bin\Debug\net8.0-windows\win-x64\SocketTestTool.dll</HintPath>
</Reference>
```

진입점에서 `App.xaml`이 하는 일을 그대로 재현합니다.

```csharp
var app = new Application { ShutdownMode = ShutdownMode.OnExplicitShutdown };
app.Resources.MergedDictionaries.Add(new ResourceDictionary {
    Source = new Uri("pack://application:,,,/SocketTestTool;component/Themes/Light.xaml", UriKind.Absolute) });
app.Resources.MergedDictionaries.Add(new ResourceDictionary {
    Source = new Uri("pack://application:,,,/SocketTestTool;component/Themes/Fluent.xaml", UriKind.Absolute) });
Encoding.RegisterProvider(CodePagesEncodingProvider.Instance);

var w = new MainWindow { WindowStartupLocation = WindowStartupLocation.Manual, Left = 20, Top = 20 };
w.Show();
var vm = (MainViewModel)w.DataContext;   // 실제 ViewModel을 그대로 사용
```

이후 `vm.Connections`에 `ConnectionModel`을 직접 넣고
`vm.StartConnectionCommand` / `SendCommand` 등 **실제 커맨드**를 실행하면 사용자 조작과 같은 경로를 탑니다.

### 6.2 반드시 필요한 유틸

- `DoEvents()` — `DispatcherFrame` + `PushFrame`으로 메시지 큐 1회 비우기
- `Pump(ms)` / `WaitUntil(조건, timeout)` — 기다리는 동안 UI를 계속 돌려야 100 ms 로그 플러시 타이머가 동작합니다
- 외부 부하용 `TcpClient` 래퍼(Blaster/RawClient)와 수집용 `TcpListener`(SinkServer)

---

## 7. 하네스 작성 시 빠지기 쉬운 함정 (전부 실제로 겪은 것)

| 함정 | 증상 | 대처 |
| --- | --- | --- |
| 빌드 산출물 경로 | `bin\Debug\net8.0-windows\`에 **2025-10-05자 구 바이너리**가 남아 있음. 실행하면 창은 뜨는데 변경사항이 하나도 반영 안 됨 | `<SelfContained>true</SelfContained>` 때문에 실제 산출물은 **`win-x64\` 하위**. 타임스탬프를 먼저 확인 |
| `ShutdownMode` | 첫 창을 닫는 순간 Application이 종료돼 이후 창 생성 실패 | `ShutdownMode.OnExplicitShutdown` |
| 로그 단언 | 방금 보낸 내용이 로그에 없다고 나옴 | 100 ms 플러시 타이머 때문. 반드시 `WaitUntil`로 감싸기 |
| `Check(name, ok, detail)` | `detail`이 null일 때 NRE | "값이 null인 것이 정상"인 검사가 있으므로 null 방어 필수 |
| CPU 측정 | 구간별 CPU가 전부 0.0%로 나옴 | 측정할 때마다 창을 초기화하면 직전 몇 ms만 잼. 읽기는 비파괴로, 초기화는 명시적으로 |
| 선택 상태 조작 | 같은 연결이 두 번 시작되어 가짜 포트 충돌 | `SelectedConnection`을 먼저 넣으면 ListView의 `SelectionChanged`가 `SelectedItems`를 다시 채움. 순서 지키기 |
| 대화상자 자동화 | 모달이라 `ShowDialog()`에서 블록 | `Show()`로 띄우고 `OkButton_Click`을 리플렉션 호출. 마지막 줄 `DialogResult = true`에서 `InvalidOperationException`이 나지만 **결과 속성은 그 전에 모두 채워져 있음** |
| `x:Name` 필드 접근 | 하네스 어셈블리에서 안 보임 | `internal`이므로 `BindingFlags.NonPublic`으로 리플렉션 |
| 메뉴 팝업 캡처 | `RenderTargetBitmap`에 팝업이 안 찍힘 | 팝업은 별도 HWND. 창을 화면에 올리고 `Graphics.CopyFromScreen`으로 **화면 전체 캡처** |
| XAML 주석 | 빌드 오류 MC3000 | XML 주석 안에 `--`를 쓸 수 없음. 구분선은 `=`로 |

---

## 8. 검증하지 못한 것 (다음 사람이 채울 부분)

- **연결 추가/수정 대화상자의 실제 버튼 클릭 흐름** — 모달이라 자동화 불가. 입력→결과 매핑과 렌더링까지만 확인했고, 사용자가 "추가"를 눌러 목록에 실제로 반영되는 마지막 한 걸음은 수동 확인 필요
- **파일 대화상자를 거치는 저장** — `Save Session` / `Save Log`의 대화상자 부분. 저장 로직 자체는 동일 경로로 검증됨
- **loopback이 아닌 실제 원격 네트워크** — 전부 127.0.0.1에서만 수행
- **TCP 포트 완전 고갈 지점** — 약 8,000쌍으로 추정되나 의도적으로 확인하지 않음 (3.3절)
- **장시간 연속 운전** — 최장 부하 구간이 수십 초 단위. 수 시간짜리 누수 확인은 미수행

---

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-08-26 | 하네스를 저장소 `qa/` 로 옮겨 상시 실행 가능하게 함. 절대 경로 제거(`RepoRoot()`), 본체 빌드에서 `qa/**` 제외. README.md 작성 |
| 2026-08-26 | 부하·자원 테스트 42개 검사 신규 작성 및 통과. 대용량 메시지 결함(#9) 발견·수정. 기능 QA 158개 재실행 통과. v2.0.1 |
| 2026-08-26 | 기능 QA 158개 검사로 확장(16개 구간). 버전 체계 v2.0.x 도입 및 `VERSIONING.md` 작성 |
| 2026-08-26 | 다크 모드 메뉴 결함(#5) 발견·수정. 중지 경합(#6)·연속 시작(#7) 결함 발견·수정 |
| 2026-08-25 | UI/UX 재설계(v2.0.0) 후 창 렌더링 스모크 테스트 및 82개 검사 QA |

---

관련 문서: [VERSIONING.md](VERSIONING.md) — 버전 자리를 올리는 기준
