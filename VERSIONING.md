# 버전 규칙 (Versioning Policy)

SYN/ACK - Socket Test Tool의 버전은 `MAJOR.MINOR.PATCH` 세 자리를 씁니다.
자리를 올리는 기준은 **작업량이나 난이도가 아니라, 사용자가 겪는 변화의 크기**입니다.
코드를 얼마나 많이 고쳤는지는 판단 근거가 아닙니다.

---

## 1. 자리별 기준

### MAJOR (맨 앞자리) — 다시 익혀야 하는 변화

다음 중 **하나라도** 해당하면 올립니다. 올릴 때 MINOR와 PATCH는 0으로 되돌립니다.

- 화면 구성(IA)이나 조작 흐름이 바뀌어, 기존 사용자가 **쓰던 방식을 다시 익혀야 하는** 경우
- 세션 파일(`.json`)이나 설정 파일의 **호환이 깨지는** 경우 (예전 파일을 못 읽거나, 읽으면 설정이 유실됨)
- 도구의 성격 자체가 바뀌는 재설계 (예: TCP 전용 → 범용 프로토콜 도구)

> **판단 기준 한 줄**: "기존 사용자에게 '바뀐 점 설명'이 필요한가?" → 필요하면 MAJOR.

### MINOR (중간자리) — 할 수 있는 일이 늘어나는 변화

- 새 기능, 새 화면, 새 옵션이 **추가**되지만 기존 조작 방식은 그대로인 경우
- 기존 기능의 동작 범위가 넓어지는 경우 (예: 지원 인코딩 추가, 응답 패턴 추가)
- 기본값이 바뀌어 **새로 만드는 연결의 동작이 달라지는** 경우 (기존 세션 파일은 영향 없음)

> **판단 기준 한 줄**: "릴리스 노트의 '주요 기능'이나 '개선 사항'에 새 항목이 생기는가?" → 생기면 MINOR.

### PATCH (마지막자리) — 원래 되어야 했던 것이 되게 만드는 변화

- 버그 수정 (동작 오류, 표시 오류, 멈춤, 자원 누수, 경합)
- 색·여백·문구처럼 **보이는 것만 바로잡는** 교정
- 사용자가 새로 할 수 있게 되는 일이 **하나도 없는** 성능 개선·내부 정리

> **판단 기준 한 줄**: "이 변경으로 사용자가 새로 할 수 있게 된 일이 있는가?" → 없으면 PATCH.

---

## 2. 헷갈릴 때의 규칙

| 상황 | 결정 |
| --- | --- |
| MAJOR/MINOR/PATCH 조건이 **섞여 있다** | 가장 높은 자리 하나만 올립니다. (예: 재설계 + 새 기능 + 버그 수정 → MAJOR 하나) |
| 큰 재설계를 했지만 **조작 방식은 그대로** | MINOR. 모양만 바뀐 것은 MAJOR가 아닙니다. |
| 작은 수정인데 **세션 파일 호환이 깨짐** | MAJOR. 코드 한 줄이어도 사용자 데이터가 걸리면 MAJOR입니다. |
| 버그를 고치면서 **기능도 하나 추가** | MINOR. 더 높은 자리가 이깁니다. |
| 아직 배포하지 않은 버전에 수정을 더함 | 자리를 올리지 않고 **그 버전에 흡수**합니다. |
| 내부 리팩토링만 하고 동작은 그대로 | 자리를 올리지 않습니다. 다음 릴리스에 묻어갑니다. |

---

## 3. 이 프로젝트의 실제 적용 사례

기준이 실제로 어떻게 적용됐는지 남겨 둡니다. 다음 판단 때 이 표와 비교하세요.

| 버전 | 내용 | 자리 | 이유 |
| --- | --- | --- | --- |
| **v2.0.5** | 연결이 없는데도 빈 세션 파일이 저장되던 문제, 연결 확인 결과의 긴 문구가 잘리던 문제 | PATCH | 둘 다 "원래 되어야 했던 것"을 고친 수정. 사용자가 새로 할 수 있게 된 일이 없음 |
| **v2.0.1** | 다크 모드 메뉴 글자 안 보임, 중지 시 Error 오표시, 연속 시작 시 가짜 포트 충돌, Hex 문자열 끝 공백 | PATCH | 전부 "원래 되어야 했던 것"을 고친 수정. 새로 할 수 있게 된 일이 없음 |
| **v2.0.0** | Windows 11 Fluent 재설계(목록→카드, 로그→표, 도킹 컴포저), 빈 상태 화면, 인라인 배너로 모달 대체, 연결 추가 대화상자 전면 개편, 다크 테마, 수신 데이터 자동 전달, 처리량 지표 | MAJOR | 화면 구성과 조작 흐름이 바뀌어 기존 사용자가 다시 익혀야 함. 새 기능이 함께 들어왔지만 **가장 높은 자리 하나만** 올려 흡수 |
| **v1.0.1** | MVVM 리팩토링, 다중 선택, 로그 뷰 개선, 순환 버퍼, 예외 처리 보강 | MINOR로 볼 수도 있었으나 PATCH로 기록됨 | 당시 기준이 없었음. 지금 기준이라면 **MINOR**가 맞습니다 |
| **v1.0.0** | 최초 릴리스 | — | — |

> v2.0.0의 "기본 인코딩 EUC-KR→ASCII", "서버 기본 주소 0.0.0.0" 변경은 단독이었다면 MINOR입니다.
> (새로 만드는 연결의 기본값만 바뀌고 기존 세션 파일은 그대로 동작하므로 MAJOR는 아닙니다.)

---

## 4. 버전을 올릴 때 고쳐야 하는 곳

버전 문자열이 흩어지지 않도록, **아래 세 곳만** 고치면 됩니다.

1. **`SocketTestTool.csproj`** — `<Version>`, `<AssemblyVersion>`, `<FileVersion>`
   실행 파일 속성과 앱 화면에 표시되는 버전이 모두 여기서 나옵니다.
2. **`app.manifest`** — `<assemblyIdentity version="...">`
   네 자리(`2.0.1.0`) 형식입니다. 마지막 자리는 항상 `0`으로 둡니다.
3. **`Models/ReleaseNote.cs`** — `ReleaseHistory.All` 맨 앞에 새 `ReleaseNote` 추가
   이전 버전의 `IsCurrent = true`는 지우고 새 항목에만 붙입니다.
   이 목록이 곧 도움말 > 버전 기록 창의 내용입니다.

상태 표시줄의 버전 표시(`MainViewModel.AppVersionText`)는 어셈블리 버전에서 자동으로 읽어 오므로
**따로 고치지 않습니다.** 손으로 적어 두면 반드시 어긋납니다.

빌드 후 다음으로 확인할 수 있습니다.

```bash
dotnet build && powershell -c "(Get-Item 'bin\Debug\net8.0-windows\win-x64\SocketTestTool.exe').VersionInfo.FileVersion"
```
