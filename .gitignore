# 빌드 산출물
bin/
obj/

# 설치 파일 산출물 (48 MB. 저장소에 넣지 않고 GitHub Releases에 올립니다)
installer/dist/

# Visual Studio 로컬 상태
.vs/
*.user

# 앱이 실행 위치에 만드는 파일 (실행 파일 옆에 생성됨)
Logs/
theme.json
recent-sessions.json

# 로컬 도구 상태·작업 파일 (공개 저장소에 포함하지 않음)
.omc/
*.zip
*.pptx
